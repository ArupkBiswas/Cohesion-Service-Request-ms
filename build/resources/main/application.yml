
spring:
  application :
    name: ServiceRequest
  datasource:
    platform: postgres
    url: jdbc:postgresql://database-1.cykmvacxmeby.ap-south-1.rds.amazonaws.com:5432/postgres
    username: postgres
    password: Hyperbola2024
    driverClassName: org.postgresql.Driver
    initialization-mode: never
    initialize: false
    separator: EOF

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

